name: Build and push Docker images

on:
  push:
    branches: [main]
  workflow_dispatch:

env:
  REGISTRY: ghcr.io

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set image metadata — backend
        id: meta-backend
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ github.repository_owner }}/852r-panel-backend
          tags: |
            type=sha,prefix=sha-
            type=raw,value=latest

      - name: Set image metadata — frontend
        id: meta-frontend
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ github.repository_owner }}/852r-panel-frontend
          tags: |
            type=sha,prefix=sha-
            type=raw,value=latest

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push backend
        uses: docker/build-push-action@v5
        with:
          context: .
          file: docker/backend.Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.meta-backend.outputs.tags }}
          labels: ${{ steps.meta-backend.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Build and push frontend
        uses: docker/build-push-action@v5
        with:
          context: .
          file: docker/frontend.Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.meta-frontend.outputs.tags }}
          labels: ${{ steps.meta-frontend.outputs.labels }}
          # Vite rank colours injected at build time from repo secrets
          build-args: |
            VITE_COLOR_STAFF=${{ secrets.VITE_COLOR_STAFF }}
            VITE_COLOR_LUMINARY=${{ secrets.VITE_COLOR_LUMINARY }}
            VITE_COLOR_PRESTIGE=${{ secrets.VITE_COLOR_PRESTIGE }}
            VITE_COLOR_VICE=${{ secrets.VITE_COLOR_VICE }}
            VITE_COLOR_SENATOR=${{ secrets.VITE_COLOR_SENATOR }}
            VITE_COLOR_DIGNITARY=${{ secrets.VITE_COLOR_DIGNITARY }}
            VITE_COLOR_ATTACHE=${{ secrets.VITE_COLOR_ATTACHE }}
            VITE_COLOR_CITIZEN=${{ secrets.VITE_COLOR_CITIZEN }}
            VITE_API_BASE_URL=
            VITE_CRAFTY_URL=${{ secrets.VITE_CRAFTY_URL }}
          cache-from: type=gha
          cache-to: type=gha,mode=max